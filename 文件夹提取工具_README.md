# 文件夹递归提取工具

将任意目录下的所有子文件夹名递归提取，导出为 Excel 文件。

## 功能特点

- 递归扫描目录下所有层级的文件夹
- 自动统计各层级文件夹数量
- 导出 Excel 包含：序号、层级、相对路径、文件夹名称、完整路径
- 支持交互式和命令行两种使用方式

## 前置要求

- Python 3.6+
- `openpyxl` 库（`pip install openpyxl`）

---

## 使用方法

### 方式一：交互式（双击运行）

直接双击运行脚本，按提示输入路径即可：

```
============================================
  文件夹递归提取工具
============================================

使用说明：
  1. 输入要扫描的目录路径
  2. 输入Excel保存路径（默认保存到源目录同级）

📂 扫描目录: E:\验货\A0151
💾 Excel保存路径 [默认: E:\验货\A0151文件夹清单_20260716_170000.xlsx]:
```

直接回车即使用默认路径。

---

### 方式二：命令行参数

#### 基本用法：扫描目录，保存到源目录同级

```bash
python 文件夹提取工具.py -s "E:\验货\A0151"
```

#### 指定输出目录

```bash
python 文件夹提取工具.py -s "E:\验货\A0151" -o "E:\WORKBUDDY\WORKSPACE"
```

#### 指定输出文件名

```bash
python 文件夹提取工具.py -s "E:\验货\A0151" -o "D:\导出" -n "我的清单"
```

此时会生成 `D:\导出\我的清单_xxx.xlsx`

#### 指定完整输出文件路径

```bash
python 文件夹提取工具.py -s "E:\验货\A0151" -o "D:\导出\result.xlsx"
```

直接指定输出文件的完整路径，文件名由 `-o` 参数中的文件名决定。

---

## 参数说明

| 参数 | 简写 | 说明 | 必填 |
|------|------|------|------|
| `--source` | `-s` | 要扫描的源目录路径 | ✅ 是 |
| `--output-dir` | `-o` | Excel 输出目录路径（或完整文件路径） | ❌ 否，默认源目录同级 |
| `--name` | `-n` | Excel 文件名前缀（不含 `.xlsx`） | ❌ 否 |

---

## Excel 输出字段

| 序号 | 层级 | 相对路径 | 文件夹名称 | 完整路径 |
|------|------|----------|------------|----------|
| 1 | 1 | `A151-001-0703（总发货单）10245件(FBA).xls` | `A151-001-0703（总发货单）10245件(FBA).xls` | `E:\验货\A0151\A151-001-0703...` |
| 2 | 2 | `A151-001-0703（总发货单）10245件(FBA).xls\AE` | `AE` | `E:\验货\A0151\A151-001...xls\AE` |
| ... | ... | ... | ... | ... |

---

## 注意事项

- 输出目录不存在时会自动创建
- 文件名不带 `.xlsx` 后缀时会自动补全
- 脚本会在运行结束后显示统计信息和前 20 个文件夹预览
