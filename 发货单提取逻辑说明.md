# 发货单 SKU 标签提取规范

## 一、概述

从一批 `.xls` / `.xlsx` 格式的发货通知单文件中，提取 **箱子编码（FBA编号）**、**发货单号**、**SKU号** 和 **对应标签**，汇总成一张总表。

## 二、文件结构

每个发货通知单文件中包含 **2 个工作表**：

| 工作表名 | 说明 |
|---------|------|
| `发货单` | **主要工作表**，包含所有需要提取的数据 |
| `packing list` | 装箱清单，本次任务不使用 |

### 2.1 关键字段位置（`发货单` 工作表）

| 字段 | 单元格 | 说明 | 示例 |
|------|--------|------|------|
| 箱子编码 | A1 | FBA 编号（如 `AE-FBA19HBZ8XRK`） | `AE-FBA19HBZ8XRK` |
| 发货单号 | A4 | 发货单号文本 | `发货单号：A151-001-0703（总发货单）10245件(FBA) 分票` |
| 表头行 | H12 / J12 | 列名："SKU号" / "对应标签" | — |
| 数据区 | H13 起 | 从第 13 行开始逐行扫描 | — |

### 2.2 数据行结构

```
行号   H列(SKU号)      J列(对应标签)
─────────────────────────────────
12     "SKU号"         "对应标签"    ← 表头
13     SOK049          X003VR04WJ    ← 数据行
14     SOB016          X003Y6AJ0J
15     （空白）        （空白）
16     SOK010          X003VR06AT
...
```

> **注意**：某些文件的表头在第 11 行（索引），数据从第 12 行开始。程序需要动态判断。

## 三、提取逻辑（核心）

### 3.1 扫描区间

- **起始行**：第 13 行（索引 12）——表头行之后
- **结束行**：最多扫描到第 1000 行（`min(1000, ws.nrows)`）
- 超过 1000 行的数据视为不可能，跳过以提高效率

### 3.2 标签覆盖机制（最重要）

J 列的"对应标签"采用**从上到下继承**的规则：

```
行13: SKU=SOK049,  J=X003VR04WJ  → activeLabel = "X003VR04WJ"
行14: SKU=SOB016,  J=X003Y6AJ0J  → activeLabel = "X003Y6AJ0J"（刷新）
行15: SKU=空白,    J=空白        → activeLabel 保持 "X003Y6AJ0J"
行16: SKU=SOK010,  J=X003VR06AT  → activeLabel = "X003VR06AT"（刷新）
行17: SKU=空白,    J=空白        → activeLabel 保持 "X003VR06AT"
...
```

**规则**：
1. 当 J 列读到**非空的新标签**时，刷新 `activeLabel`
2. 当 J 列为空时，`activeLabel` 保持上一次的值不变
3. 如果连续 **10 行** H 列都为空，认为明细区域已结束，清空 `activeLabel`

### 3.3 过滤条件

每一行必须**同时满足以下三个条件**才会被提取：

| 条件 | 说明 |
|------|------|
| ① SKU 不为空 | H 列必须有值 |
| ② SKU 不含中文 | 使用 `ord(char) > 255` 判断，排除中文汉字 |
| ③ 有有效标签 | `activeLabel` 不能为空 |

### 3.4 数据类型处理

Excel 单元格可能包含多种类型：

| xlrd 类型 | 常量 | 处理方式 |
|-----------|------|----------|
| 空单元格 | `XL_CELL_EMPTY` | 返回空字符串 |
| 文本 | `XL_CELL_TEXT` | 转为字符串并去除前后空格 |
| 数字 | `XL_CELL_NUMBER` | 若为整数则去掉小数点（如 `2.0` → `2`） |

## 四、输出格式

### 4.1 汇总表结构

| 列名 | 来源 | 说明 |
|------|------|------|
| 箱子编码 | A1 | FBA 编号 |
| 发货单号 | A4 | 发货单号全文 |
| SKU号 | H列 | 第 8 列的 SKU 编号 |
| 对应标签 | J列 | 第 10 列的标签（继承机制） |
| 源文件名 | — | 原始文件名称 |

### 4.2 示例数据

| 箱子编码 | 发货单号 | SKU号 | 对应标签 | 源文件名 |
|----------|---------|-------|---------|---------|
| AE-FBA19HBZ8XRK | 发货单号：A151-001-0703... | SOK010 | X003VR06AT | A151-...XLX1.xls |
| AE-FBA19HBZ8XRK | 发货单号：A151-001-0703... | SOK012 | X003VQZZPV | A151-...XLX1.xls |

## 五、技术细节

### 5.1 使用的库

| 库 | 用途 |
|----|------|
| `xlrd` | 读取 `.xls` 格式的 Excel 文件 |
| `pandas` | 构建 DataFrame 并导出 `.xlsx` |
| `openpyxl` | pandas 写入 `.xlsx` 的引擎 |

### 5.2 合并单元格处理

发货通知单文件中可能存在合并单元格，但 `xlrd` 读取时合并区域的第一个单元格保留值，其余为空白。当前逻辑直接读取目标单元格即可，无需特殊处理。

### 5.3 中文检测函数

```python
def has_chinese(text):
    """判断字符串是否包含中文字符（Unicode > 255）"""
    for char in text:
        if ord(char) > 255:
            return True
    return False
```

这与 VBA 中的 `AscW(mid(text, i, 1)) > 255` 逻辑完全一致。

## 六、流程图解

```
┌─────────────────────────┐
│  遍历 input the list 文件夹 │
│  找出所有 .xls / .xlsx     │
└───────────┬─────────────┘
            ▼
┌─────────────────────────┐
│  打开每个文件             │
│  检查是否有"发货单"工作表   │
│  如果没有 → 跳过          │
└───────────┬─────────────┘
            ▼
┌─────────────────────────┐
│  读取 A1 = 箱子编码        │
│  读取 A4 = 发货单号        │
└───────────┬─────────────┘
            ▼
┌─────────────────────────┐
│  初始化:                  │
│  activeLabel = ""         │
│  emptyRowCnt = 0          │
└───────────┬─────────────┘
            ▼
┌─────────────────────────┐
│  从第13行扫描到第1000行    │
│  （或直到 ws.nrows 为止）   │
└───────────┬─────────────┘
            ▼
  ┌─────────────────────────┐
  │  H列 = SKU号             │
  │  J列 = 对应标签           │
  └────┬──────────────┬─────┘
       │              │
       ▼              ▼
  ┌─────────┐    ┌──────────┐
  │J列有值?  │    │H列有空?  │
  └────┬────┘    └────┬─────┘
       │              │
    是→刷新         是→计数器+1
   activeLabel     ┌──┴───┐
                   │≥10行?│
                   └──┬───┘
                     │
                  是→清空activeLabel
                     │
                     ▼
              ┌──────────────┐
              │判断是否写入:   │
              │ ① H≠空        │
              │ ② H无中文      │
              │ ③ activeLabel≠空│
              └──────┬───────┘
                     │
                  全部满足→写入一行记录
                     │
                     ▼
              ┌──────────────┐
              │  所有文件处理完 │
              │  汇总导出 .xlsx │
              └──────────────┘
```

## 七、配置参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `INPUT_FOLDER` | `E:\验货\A0151\input the list` | 输入文件夹路径 |
| `OUTPUT_FOLDER` | `E:\验货\A0151\汇总结果_v3` | 输出文件夹路径 |
| `OUTPUT_FILENAME` | `发货单_SKU标签汇总.xlsx` | 输出文件名 |
| `DATA_START_ROW` | `12` | 数据起始行（索引从0开始，12=第13行） |
| `LABEL_OVERRIDE_THRESHOLD` | `10` | 连续空白行数阈值，超过后清空标签 |

## 八、注意事项

1. **文件扩展名**：程序同时支持 `.xls`（旧格式，用 `xlrd` 读）和 `.xlsx`（新格式）
2. **标签继承**：这是最关键的特性——J列标签不是逐行独立的，而是"读到新值才变"
3. **中文过滤**：有些行H列可能包含中文描述（如"装箱支数"），这些行会被自动跳过
4. **性能优化**：每行最多扫描到第1000行，避免处理超大文件
5. **错误容错**：单个文件读取失败不影响其他文件，错误信息会汇总输出
